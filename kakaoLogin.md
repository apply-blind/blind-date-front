# 카카오 소셜 로그인 구현 가이드

## 📌 목차
1. [카카오 개발자 콘솔 설정](#1-카카오-개발자-콘솔-설정)
2. [OAuth2 로그인 동작 워크플로우](#2-oauth2-로그인-동작-워크플로우)
3. [구현 단계](#3-구현-단계)
4. [테스트 방법](#4-테스트-방법)

---

## 1. 카카오 개발자 콘솔 설정

### ✅ 완료된 설정 단계

#### Step 1. 카카오 개발자 콘솔 접속
- URL: https://developers.kakao.com/
- 카카오 계정으로 로그인

#### Step 2. 애플리케이션 추가
1. **내 애플리케이션** 메뉴 클릭
2. **애플리케이션 추가하기** 버튼 클릭
3. 앱 정보 입력:
   - **앱 이름**: `toastnjam` (원하는 이름 입력)
   - **사업자명**: 개인 이름 입력
4. **저장** 버튼 클릭

#### Step 3. 앱 키 확인
1. **내 애플리케이션** > 생성한 앱 선택
2. **앱 설정** > **앱 키** 메뉴 이동
3. 다음 키 복사:
   - ✅ **REST API 키**: `fdb051a46d97e02e3b1332eb1c48f232`
   - ✅ **JavaScript 키**: `9f53c7da1ec29d59a667049e0f8a95f7`

#### Step 4. 플랫폼 설정
1. **앱 설정** > **플랫폼** 메뉴 이동
2. **Web 플랫폼 등록** 버튼 클릭
3. **사이트 도메인** 입력:
   ```
   http://localhost:8080
   ```
4. **저장** 버튼 클릭

> **💡 참고**: 추후 배포 시 실제 도메인도 추가 등록 필요

#### Step 5. 카카오 로그인 활성화
1. **제품 설정** > **카카오 로그인** 메뉴 이동
2. **카카오 로그인 활성화** 토글 **ON**
3. **Redirect URI** 설정:
   - **Redirect URI 등록** 버튼 클릭
   - URI 입력:
     ```
     http://localhost:8080/login/oauth2/code/kakao
     ```
   - **저장** 버튼 클릭

> **🔍 Redirect URI 설명**:
> - 형식: `{baseUrl}/login/oauth2/code/{registrationId}`
> - Spring Security OAuth2 표준 규격
> - `{registrationId}`: `kakao` (application.yml에서 설정할 이름)
> - 이 URL로 컨트롤러를 만들 필요 없음 (Spring Security가 자동 처리)

#### Step 6. Client Secret 발급 (보안 강화)
1. **제품 설정** > **카카오 로그인** > **보안** 탭 이동
2. **Client Secret** 섹션에서 **코드 생성** 버튼 클릭
3. 생성된 Secret 복사:
   - ✅ **Client Secret**: `LDMaWQtXOvRgKp1Z4qx8zLjwmIXY5Uc6`
4. **활성화 상태**를 **사용함**으로 변경
5. **저장** 버튼 클릭

> **⚠️ 주의**: Client Secret은 절대 외부에 노출하면 안 됩니다!

#### Step 7. 동의항목 설정
1. **제품 설정** > **카카오 로그인** > **동의항목** 메뉴 이동
2. 다음 항목 설정:

| 항목 | ID | 설정값 | 이유 |
|------|-----|--------|------|
| **닉네임** | `profile_nickname` | **필수 동의** | 사용자 식별 및 화면 표시에 필수 |
| **프로필 사진** | `profile_image` | **선택 동의** | 가입 장벽 완화 (별도 프로필 심사 있음) |
| ~~카카오계정(이메일)~~ | `account_email` | ❌ **권한 없음** | 비즈니스 채널 전환 필요 (현재 사용 불가) |
| ~~카카오 서비스 내 친구목록~~ | `friends` | ❌ **미설정** | 단순 공유하기는 권한 불필요 |

3. 각 항목의 **설정** 버튼을 눌러 동의 레벨 변경
4. **저장** 버튼 클릭

#### 📋 설정 완료 확인
- [x] 앱 생성
- [x] REST API 키, Client Secret 발급
- [x] Web 플랫폼 등록 (localhost:8080)
- [x] Redirect URI 등록
- [x] 동의항목 설정 (닉네임, 프로필 사진)

---

## 2. OAuth2 로그인 동작 워크플로우

### 🔄 전체 플로우 (10단계)

```
┌─────────────┐
│   사용자     │
└──────┬──────┘
       │
       │ [1] 카카오 로그인 버튼 클릭
       │     (GET /oauth2/authorization/kakao)
       ↓
┌──────────────────────┐
│  Spring Security     │
│  OAuth2 Client       │
└──────┬───────────────┘
       │
       │ [2] 카카오 인증 페이지로 리다이렉트
       │     + client_id
       │     + redirect_uri
       │     + response_type=code
       ↓
┌──────────────────────┐
│   카카오 인증 서버    │
│ (kauth.kakao.com)    │
└──────┬───────────────┘
       │
       │ [3] 사용자 로그인 & 동의
       │     (카카오톡 QR, 이메일 로그인 등)
       ↓
┌──────────────────────┐
│   사용자 동의 화면    │
│  (닉네임, 프로필 등)  │
└──────┬───────────────┘
       │
       │ [4] 동의 완료 → 인증 코드 발급
       │     Redirect: /login/oauth2/code/kakao?code=xxx
       ↓
┌──────────────────────┐
│  Spring Security     │
│  (자동 처리)         │
└──────┬───────────────┘
       │
       │ [5] 인증 코드로 액세스 토큰 요청
       │     POST https://kauth.kakao.com/oauth/token
       │     - grant_type=authorization_code
       │     - client_id
       │     - client_secret
       │     - code
       │     - redirect_uri
       ↓
┌──────────────────────┐
│   카카오 인증 서버    │
└──────┬───────────────┘
       │
       │ [6] 액세스 토큰 발급
       │     { access_token, token_type, expires_in, ... }
       ↓
┌──────────────────────┐
│  Spring Security     │
└──────┬───────────────┘
       │
       │ [7] 액세스 토큰으로 사용자 정보 요청
       │     GET https://kapi.kakao.com/v2/user/me
       │     Authorization: Bearer {access_token}
       ↓
┌──────────────────────┐
│   카카오 API 서버     │
└──────┬───────────────┘
       │
       │ [8] 사용자 정보 응답
       │     {
       │       id: 카카오 고유 번호,
       │       properties: { nickname, profile_image },
       │       kakao_account: { ... }
       │     }
       ↓
┌──────────────────────┐
│ CustomOAuth2User     │
│ Service (우리 코드)   │
└──────┬───────────────┘
       │
       │ [9] 사용자 정보 처리
       │     - DB에 사용자 저장/업데이트
       │     - User 엔티티 생성
       │     - 신규 가입 vs 기존 회원 판별
       ↓
┌──────────────────────┐
│ OAuth2Success        │
│ Handler (우리 코드)   │
└──────┬───────────────┘
       │
       │ [10] JWT 토큰 발급 & 리다이렉트
       │      - JWT 생성 (user_id, provider 정보 포함)
       │      - 프론트엔드로 리다이렉트 (토큰 전달)
       │      - 예: /auth/success?token=xxx
       ↓
┌──────────────────────┐
│   사용자 (로그인 완료) │
│   JWT 토큰 보유       │
└──────────────────────┘
       │
       │ 이후 모든 API 요청
       │ Authorization: Bearer {JWT}
       ↓
┌──────────────────────┐
│ JwtAuthentication    │
│ Filter (우리 코드)    │
└──────────────────────┘
       - JWT 검증
       - SecurityContext 설정
       - API 접근 허용
```

### 🔑 핵심 개념

#### OAuth2 Authorization Code Grant 방식
- 가장 안전한 OAuth2 인증 방식
- 서버 간 통신으로 토큰 교환 (브라우저에 노출 안 됨)
- Authorization Code → Access Token 2단계 인증

#### Spring Security가 자동 처리하는 부분
- [2] 인증 페이지 리다이렉트
- [5] 인증 코드로 액세스 토큰 요청
- [7] 액세스 토큰으로 사용자 정보 요청
- Redirect URI 처리 (`/login/oauth2/code/kakao`)

#### 우리가 구현해야 하는 부분
- [8~9] CustomOAuth2UserService: 사용자 정보 처리
- [10] OAuth2SuccessHandler: JWT 발급
- JWT 토큰 생성/검증 로직
- JwtAuthenticationFilter: 이후 요청에서 JWT 검증

---

## 3. 구현 단계

### 📝 구현 체크리스트

#### ✅ 완료
- [x] 프로젝트 초기 설정
- [x] JWT 의존성 추가 (jjwt 0.12.6)
- [x] 카카오 개발자 콘솔 설정
- [x] Git 초기 커밋

#### 🔄 진행 예정 (순서대로)

##### 1단계: 데이터베이스 및 엔티티
- [ ] `User` 엔티티 생성
  - `id` (Long, PK)
  - `name` (String, 카카오 닉네임)
  - `provider` (String, "kakao")
  - `providerId` (String, 카카오 고유 ID)
  - `profileImageUrl` (String, 프로필 사진 URL)
  - `createdAt`, `updatedAt` (Timestamp)
- [ ] `UserRepository` 인터페이스 생성
- [ ] Flyway 마이그레이션 파일 작성 (선택사항)

##### 2단계: Spring Security 설정
- [ ] `application.yml`에 OAuth2 설정 추가
  ```yaml
  spring:
    security:
      oauth2:
        client:
          registration:
            kakao:
              client-id: ${KAKAO_CLIENT_ID}
              client-secret: ${KAKAO_CLIENT_SECRET}
              ...
  ```
- [ ] JWT 설정 추가
  ```yaml
  jwt:
    secret: ${JWT_SECRET}
    expiration: 86400000  # 24시간
  ```

##### 3단계: JWT 구현
- [ ] `JwtTokenProvider` 클래스 생성
  - `generateToken(User)`: JWT 생성
  - `validateToken(String)`: JWT 검증
  - `getUserIdFromToken(String)`: JWT에서 사용자 ID 추출

##### 4단계: OAuth2 사용자 정보 처리
- [ ] `CustomOAuth2UserService` 클래스 생성
  - `DefaultOAuth2UserService` 상속
  - `loadUser()` 메서드 오버라이드
  - 카카오 사용자 정보 파싱
  - DB에 사용자 저장/업데이트

##### 5단계: 로그인 성공 처리
- [ ] `OAuth2SuccessHandler` 클래스 생성
  - `SimpleUrlAuthenticationSuccessHandler` 상속
  - `onAuthenticationSuccess()` 메서드 오버라이드
  - JWT 토큰 생성
  - 프론트엔드로 리다이렉트 (토큰 포함)

##### 6단계: JWT 인증 필터
- [ ] `JwtAuthenticationFilter` 클래스 생성
  - `OncePerRequestFilter` 상속
  - 요청 헤더에서 JWT 추출
  - JWT 검증 및 SecurityContext 설정

##### 7단계: Security 설정
- [ ] `SecurityConfig` 클래스 생성
  - `SecurityFilterChain` Bean 등록
  - OAuth2 로그인 설정
  - JWT 필터 추가
  - 인증/인가 규칙 정의
  - CORS 설정

##### 8단계: 테스트
- [ ] 카카오 로그인 플로우 테스트
- [ ] JWT 발급 확인
- [ ] 인증된 API 접근 테스트

---

## 4. 테스트 방법

### 로컬 테스트 순서

#### 1. 애플리케이션 실행
```bash
./gradlew bootRun --args='--spring.profiles.active=local'
```

#### 2. 카카오 로그인 시작
브라우저에서 접속:
```
http://localhost:8080/oauth2/authorization/kakao
```

#### 3. 카카오 로그인 진행
- 카카오톡 QR 코드 스캔 또는 이메일 로그인
- 동의 항목 확인 및 동의
- 자동으로 `http://localhost:8080/login/oauth2/code/kakao?code=xxx`로 리다이렉트

#### 4. JWT 토큰 확인
- 로그인 성공 후 리다이렉트된 URL에서 JWT 토큰 확인
- 브라우저 개발자 도구 > Application > Local Storage 확인

#### 5. 인증된 API 호출 테스트
```bash
curl -H "Authorization: Bearer {JWT_TOKEN}" \
     http://localhost:8080/api/v1/users/me
```

### 예상 응답
```json
{
  "id": 1,
  "name": "홍길동",
  "provider": "kakao",
  "profileImageUrl": "https://...",
  "createdAt": "2025-11-20T00:00:00"
}
```

---

## 📚 참고 자료

### 카카오 개발 문서
- [카카오 로그인 개요](https://developers.kakao.com/docs/latest/ko/kakaologin/common)
- [REST API 가이드](https://developers.kakao.com/docs/latest/ko/kakaologin/rest-api)
- [사용자 정보 가져오기](https://developers.kakao.com/docs/latest/ko/kakaologin/rest-api#req-user-info)

### Spring Security 문서
- [OAuth2 Client](https://docs.spring.io/spring-security/reference/servlet/oauth2/client/index.html)
- [OAuth2 Login](https://docs.spring.io/spring-security/reference/servlet/oauth2/login/index.html)

### JWT 라이브러리
- [jjwt GitHub](https://github.com/jwtk/jjwt)

---

## ⚠️ 주의사항

### 보안
1. **민감 정보는 환경변수로 관리**
   - `KAKAO_CLIENT_ID`
   - `KAKAO_CLIENT_SECRET`
   - `JWT_SECRET`
   - `.env` 파일 사용 또는 시스템 환경변수 설정

2. **JWT Secret 생성**
   ```bash
   # 256비트 랜덤 문자열 생성
   openssl rand -base64 32
   ```

3. **Git에 절대 커밋하지 말 것**
   - application-local.yml (이미 .gitignore에 추가됨)
   - .env 파일

### 카카오 API 제한사항
- **이메일**: 비즈니스 채널 전환 필요 (현재 사용 불가)
- **전화번호**: 비즈니스 채널 전환 필요 (현재 사용 불가)
- **CI**: 심사 필요 (현재 사용 불가)
- ➡️ **대안**: 추후 별도 휴대폰 본인 인증 API 연동 예정

### 데이팅 앱 특화 사항
- 카카오 프로필 사진은 참고용
- **실제 프로필은 3장 업로드 + 관리자 심사** (PRD 참고)
- 본인 인증은 휴대폰 인증으로 진행 예정

---

## 🚀 다음 단계

1. **User 엔티티 및 도메인 모델 생성**부터 시작
2. 순차적으로 구현 진행
3. 각 단계마다 테스트 후 Git 커밋

**구현 준비 완료!**